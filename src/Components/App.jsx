import React from "react";
import { Typography } from "@material-ui/core";

const App = (props) => {
  return (
    <div>
      <Typography variant="h3">
      HoneyBadger
      </Typography>
    </div>
  );
};

export default App;
